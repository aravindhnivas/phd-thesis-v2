\let\citet\textcite
\addbibresource{dissertation.bib} % only used references

\AtBeginBibliography{%
  \urlstyle{rm}%
}

\renewbibmacro{in:}{}
\AtEveryBibitem{%
  \ifentrytype{misc}{%
  }{%
    \clearfield{url}%
    \clearfield{urldate}%
    \clearfield{note}%
    \clearfield{series}%
    \clearfield{month}%
    \clearfield{ISSN}%
  }%
}

% \DeclareFieldFormat[article]{title}{#1}
% \DeclareFieldFormat[book]{title}{\mkbibemph{#1}}

% legacy year field '{2018}' in entry '[cite name]' is not an integer - this will probably not sort properly.
\DeclareSourcemap{
  \maps[datatype=bibtex]{
    \map{
      \step[fieldsource=year, match=\regexp{\{(\d{4})\}}, replace=$1]
    }
  }
}
